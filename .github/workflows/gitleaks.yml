name: Secret Scanning

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  gitleaks:
    uses: Saronic-Security/.github/.github/workflows/gitleaks.yml@main
    permissions:
      contents: read
      security-events: write
      actions: read
